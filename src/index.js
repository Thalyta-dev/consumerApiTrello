import React from 'react';
import ReactDOM from 'react-dom';
import reportWebVitals from './reportWebVitals';
import Api from './form'
  ReactDOM.render(
  <React.StrictMode>
    <Api/>
  </React.StrictMode>,
  document.getElementById('root'));
